import {productSchema, IProduct} from "../models/orderModel";

// async function createProduct(productData: IProduct): Promise<IProduct> {
//     const product = new productSchema(productData);
//     return await product.save();

    
// }